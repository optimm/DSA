#include <bits/stdc++.h>
using namespace std;
int main()
{
    // queue<int> q;
    // stack<int> st;
    // int k = 4;

    // q.push(1);
    // q.push(2);
    // q.push(3);
    // q.push(4);
    // q.push(5);
    // int y = q.size() - k;

    // while (k--)
    // {
    //     st.push(q.front());
    //     q.pop();
    // }
    // while (!st.empty())
    // {
    //     q.push(st.top());
    //     st.pop();
    // }
    // while (y--)
    // {
    //     q.push(q.front());
    //     q.pop();
    // }
    // while (!q.empty())
    // {
    //     cout << q.front() << " ";
    //     q.pop();
    // }

    cout << 0.1 + 0.2 << "\n";

    return 0;
}