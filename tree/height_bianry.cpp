#include <bits/stdc++.h>
#define ll long long
using namespace std;

// int height(Node *node)
// {
//     if (node == NULL)
//         return 0;
//     int lh = height(node->left);
//     int rh = height(node->right);
//     return 1 + max(lh, rh);
// }

int main()
{
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    return 0;
}