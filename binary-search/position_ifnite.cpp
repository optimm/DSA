#include <bits/stdc++.h>
using namespace std;
// isme 2-2-2 SE * kar karke dekh lenge ki kab tak isme bada nahi aajata target se fir binary_search laga do and first ko prev last se update kardo
int main()
{
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    return 0;
}